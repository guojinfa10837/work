import * as actionTypes from './actionTypes';
export const searchFocus = ()=>({
     type:actionTypes.HEAD_SEARCH_FOCUSED
});
export const searchBlur = ()=>({
    type:actionTypes.HEAD_SEARCH_BLURED
});